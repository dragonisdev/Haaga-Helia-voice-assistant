[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPaths = ["agent.py", "download_models.py", "requirements.txt", "Dockerfile"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 2
