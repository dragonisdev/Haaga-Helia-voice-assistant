[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPaths = ["agent/agent.py", "agent/requirements.txt", "agent/Dockerfile"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 2
